%!PS-Adobe-2.0
%%Creator: Sergei S. Betke
%%EndComments

/reencodefont % newfontname font encoding reencodefont --> font
{
  exch dup type dup /nametype eq exch /stringtype eq or { findfont } if
  dup length dict begin
    {
      1 index /FID ne 2 index /UniqueID ne and { def } { pop pop } ifelse
    } forall
    /Encoding exch dup type /nametype eq { /Encoding findresource } if def
    dup /FontName exch def
    currentdict
  end
  definefont
} bind def

%%EOF
