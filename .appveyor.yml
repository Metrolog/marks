image:
  - WMF 5
  - Ubuntu

version: 2.0.0.{build}

install:
  - sh: |
      sudo apt-get install -y make ghostscript
  - ps: |
      if ( -not $isLinux ) {
        .\install\install.ps1 -ErrorAction Stop -InformationAction Continue -Verbose
      }

build_script:
  - make
